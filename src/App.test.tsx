import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

test('renders All your moonpie are belong to us. link', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/All your moonpie are belong to us./i);
//   expect(linkElement).toBeInTheDocument();
});
